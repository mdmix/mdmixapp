<%= form_with(model: user, local: true, class:"user-form") do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :first_name, :class => "name-label" %>
    <%= form.text_field :first_name, :class => "user-names" %>
  </div>

  <div class="field">
    <%= form.label :last_name, :class => "name-label" %>
    <%= form.text_field :last_name, :class => "user-names" %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
